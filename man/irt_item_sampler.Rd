% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{irt_item_sampler}
\alias{irt_item_sampler}
\title{2PL IRT item-parameter sampler (WALNUTS)}
\usage{
irt_item_sampler(
  responses,
  alpha,
  beta,
  beta_sd = 10,
  step_size = 0.1,
  seed = 1L
)
}
\arguments{
\item{responses}{Integer/numeric matrix of 0/1 item responses, persons in
rows and items in columns (\code{n_persons} x \code{n_items}). Coerced to double.}

\item{alpha}{Numeric vector of initial discriminations, length \code{n_items},
all positive.}

\item{beta}{Numeric vector of initial difficulties, length \code{n_items}.}

\item{beta_sd}{Prior standard deviation for the item difficulties.}

\item{step_size}{Initial leapfrog step size for adaptation.}

\item{seed}{Integer seed for the sampler's own random-number generator (it is
independent of R's RNG).}
}
\value{
An object of class \code{"irt_item_sampler"} wrapping an external pointer
to the compiled sampler, plus the \code{n_persons} and \code{n_items} dimensions.
}
\description{
Creates a persistent sampler for the item parameters of a two-parameter
logistic (2PL) item-response model, conditional on the person abilities
\code{theta}. The item parameters --- discrimination \code{alpha} (positive, with an
\code{Exponential(1)} prior, sampled on the log scale) and difficulty \code{beta}
(\code{Normal(0, beta_sd)}) --- are drawn by the gradient-based WALNUTS sampler.
}
\details{
The intended lifecycle, holding \code{theta} fixed within each draw but letting it
change between draws, is: create the sampler once, call \code{\link[=irt_warmup]{irt_warmup()}} once
per outer warm-up scan (adaptation of step size and mass), \code{\link[=irt_freeze]{irt_freeze()}}
once to hand off to fixed tuning, then \code{\link[=irt_draw]{irt_draw()}} once per kept scan. See
\code{\link[=irt_causal_bart]{irt_causal_bart()}} for the full model that drives it.
}
\seealso{
\code{\link[=irt_warmup]{irt_warmup()}}, \code{\link[=irt_freeze]{irt_freeze()}}, \code{\link[=irt_draw]{irt_draw()}}, \code{\link[=irt_tuning]{irt_tuning()}}
}
