% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{irt_freeze}
\alias{irt_freeze}
\title{Freeze an IRT item-parameter sampler}
\usage{
irt_freeze(sampler)
}
\arguments{
\item{sampler}{An \code{"irt_item_sampler"} from \code{\link[=irt_item_sampler]{irt_item_sampler()}}.}
}
\value{
The \code{sampler}, invisibly.
}
\description{
Ends adaptation and hands the adapted tuning to a fixed-tuning sampler. Call
once, after the last \code{\link[=irt_warmup]{irt_warmup()}} and before the first \code{\link[=irt_draw]{irt_draw()}}. It is
an error to freeze twice. Freezing before any \code{\link[=irt_warmup]{irt_warmup()}} call (i.e. with
\code{irt_tuning(sampler)$warmup_iter == 0}) is allowed but skips adaptation
entirely, leaving the sampler at its initial \code{step_size} and identity mass.
}
\seealso{
\code{\link[=irt_item_sampler]{irt_item_sampler()}}
}
