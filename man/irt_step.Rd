% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{irt_step}
\alias{irt_step}
\alias{irt_warmup}
\alias{irt_draw}
\title{Advance an IRT item-parameter sampler}
\usage{
irt_warmup(sampler, theta)

irt_draw(sampler, theta)
}
\arguments{
\item{sampler}{An \code{"irt_item_sampler"} from \code{\link[=irt_item_sampler]{irt_item_sampler()}}.}

\item{theta}{Numeric vector of person abilities, length \code{n_persons}.}
}
\value{
A numeric vector of length \code{2 * n_items}: \code{c(alpha, beta)}.
}
\description{
\code{irt_warmup()} takes one adapting step (step size and mass are updated) and
\code{irt_draw()} one fixed-tuning step; both condition on the supplied \code{theta}
and return the current \code{c(alpha, beta)} draw on the natural scale (\code{alpha}
first, then \code{beta}, each of length \code{n_items}). Call \code{\link[=irt_freeze]{irt_freeze()}} once
between the warm-up and sampling phases.
}
\details{
Warm-up draws are not valid for inference --- they exist to condition the
rest of an outer warm-up scan while the tuning adapts.
}
\seealso{
\code{\link[=irt_item_sampler]{irt_item_sampler()}}
}
